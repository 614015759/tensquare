spring:
  cloud:
    config:
      name: web
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: tensquare-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6869/eureka